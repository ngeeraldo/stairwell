// Tailwind v4's PostCSS plugin. Three lines, and they should never need to
// change: v4 does its configuration in CSS (app/globals.css), not here.
const config = { plugins: { '@tailwindcss/postcss': {} } }

export default config
